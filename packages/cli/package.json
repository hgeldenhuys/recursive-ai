{
  "name": "@recursive-ai/cli",
  "version": "0.1.0",
  "description": "Bootstrap and management CLI for SWARM SDLC",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "recursive": "src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@recursive-ai/core": "workspace:*",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.7.0"
  }
}
