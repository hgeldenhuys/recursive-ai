{
  "name": "@recursive-ai/marketplace",
  "version": "0.1.0",
  "description": "Plugin registry and manifest validation for SWARM marketplace",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@recursive-ai/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.7.0"
  }
}
